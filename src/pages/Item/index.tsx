import styles from './styles.module.scss';

export const Item = () => {
    return (
        <main className={styles.wrapper}>
            <div>
                
            </div>
        </main>
    )
}